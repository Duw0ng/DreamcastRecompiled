--- a/src/codegen/cpp_emitter.cpp
+++ b/src/codegen/cpp_emitter.cpp
@@ -2350,7 +2350,7 @@
     std::uint32_t current_pc{};
     bool trace_calls{};
     bool trace_history_enabled{};
-    // 0.0.204 diagnostic-only SH-4 ABI checker. When enabled the fast dynamic
+    // 0.0.205 diagnostic-only SH-4 ABI checker. When enabled the fast dynamic
     // dispatch path is disabled so every indirect call can verify callee-saved
     // R8-R14 and SP on an ordinary return. Non-local/context-switch returns are
     // deliberately skipped.
@@ -3240,6 +3240,21 @@
     return physical >= DCRuntime::kHollyPhysicalBase && physical < DCRuntime::kHollyPhysicalEnd;
 }
 
+// Area-0 external apertures. Dreamcast modem registers occupy
+// 0x00600000-0x006007FF. G2 external devices occupy
+// 0x01000000-0x01FFFFFF. When no external device is attached, Flycast's
+// baseline returns zero for reads and ignores writes; model that open-bus
+// behaviour instead of treating these legal hardware apertures as unmapped.
+bool is_modem_aperture(std::uint32_t address) {
+    const auto physical = physical29(address);
+    return physical >= 0x00600000u && physical < 0x00600800u;
+}
+
+bool is_g2_external_aperture(std::uint32_t address) {
+    const auto physical = physical29(address);
+    return physical >= 0x01000000u && physical < 0x02000000u;
+}
+
 bool is_ta_fifo(std::uint32_t address) {
     const auto physical = physical29(address);
     return physical >= kTaFifoPhysicalBase && physical < kTaFifoPhysicalEnd;
@@ -12062,6 +12077,7 @@
 
 std::uint8_t dc_read8(DCRuntime& runtime, std::uint32_t address) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return 0u;
     std::size_t i = 0;
     if (onchip_ram_range(address, 1, i)) return runtime.onchip_ram[i];
     if (boot_rom_low_range(address, 1, i)) {
@@ -12084,6 +12100,7 @@
 
 std::uint16_t dc_read16(DCRuntime& runtime, std::uint32_t address) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return 0u;
     std::size_t i = 0;
     if (onchip_ram_range(address, 2, i)) {
         return static_cast<std::uint16_t>(runtime.onchip_ram[i + 0]) |
@@ -12121,6 +12138,7 @@
 
 std::uint32_t dc_read32(DCRuntime& runtime, std::uint32_t address) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return 0u;
     std::size_t i = 0;
     if (onchip_ram_range(address, 4, i)) {
         return static_cast<std::uint32_t>(runtime.onchip_ram[i + 0]) |
@@ -12167,6 +12185,7 @@
 
 void dc_write8(DCRuntime& runtime, std::uint32_t address, std::uint8_t value) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return;
     // P4 Store Queue addresses must be decoded before applying SH-4 P1/P2/P3
     // physical aliases. For example, 0xE0800000 is a Store Queue aperture that
     // may target AICA RAM after PREF; it is not a direct write to 0x00800000.
@@ -12197,6 +12216,7 @@
 
 void dc_write16(DCRuntime& runtime, std::uint32_t address, std::uint16_t value) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return;
     if (is_store_queue(address)) {
         dc_write8(runtime, address + 0u, static_cast<std::uint8_t>(value & 0xFFu));
         dc_write8(runtime, address + 1u, static_cast<std::uint8_t>((value >> 8) & 0xFFu));
@@ -12242,6 +12262,7 @@
 
 void dc_write32(DCRuntime& runtime, std::uint32_t address, std::uint32_t value) {
     address = sh4_translate_guest_address(runtime, address);
+    if (is_modem_aperture(address) || is_g2_external_aperture(address)) return;
     if (is_store_queue(address)) {
         ++runtime.sq_writes;
         const std::size_t base = static_cast<std::size_t>((address >> 5) & 1u) * 32u + (address & 31u);
@@ -13479,7 +13500,7 @@
                       << ((runtime.perf_pvr_clock_sampled_ns * std::max<std::uint64_t>(1u, runtime.perf_sample_stride)) / 1000000u) << "/"
                       << ((runtime.perf_host_sync_sampled_ns * std::max<std::uint64_t>(1u, runtime.perf_sample_stride)) / 1000000u) << "/"
                       << ((runtime.perf_irq_sampled_ns * std::max<std::uint64_t>(1u, runtime.perf_sample_stride)) / 1000000u)
-                      << " | build=0.0.204"
+                      << " | build=0.0.205"
                       << " | baseline=0.0.124"
                       << " | pvr-fast=" << (runtime.pvr_gpu_fastpath_ready ? 1 : 0)
                       << "/" << (runtime.pvr_mt_fastpath_ready ? 1 : 0)
@@ -20230,7 +20251,7 @@
                    << "#endif\n"
                    << "    std::ostringstream out;\n"
                    << "    if (!ec) out << \"logs/\";\n"
-                   << "    out << \"DreamcastRecomp_0.0.204_session_\" << std::put_time(&tm, \"%Y%m%d-%H%M%S\") << \".log\";\n"
+                   << "    out << \"DreamcastRecomp_0.0.205_session_\" << std::put_time(&tm, \"%Y%m%d-%H%M%S\") << \".log\";\n"
                    << "    return out.str();\n"
                    << "}\n\n"
                    << "class DCRSessionLogGuard final {\n"
@@ -20540,12 +20561,12 @@
                    << "        runtime.fast_dispatch_enabled = fast_dispatch;\n"
                    << "        runtime.direct_dispatch_enabled = direct_dispatch;\n"
                    << "        dc_refresh_dispatch_ready(runtime);\n"
-                   << "        if (pvr_window) dc_pvr_enable_window(runtime, \"DreamcastRecomp 0.0.204 - PVR live\", pvr_window_scale, pvr_window_interval, pvr_window_throttle_ms, pvr_window_fps);\n"
+                   << "        if (pvr_window) dc_pvr_enable_window(runtime, \"DreamcastRecomp 0.0.205 - PVR live\", pvr_window_scale, pvr_window_interval, pvr_window_throttle_ms, pvr_window_fps);\n"
                    << "        runtime.device_clock_host_max_catchup_steps = device_clock_host_max_catchup;\n"
                    << "        if (device_clock) dc_device_clock_enable(runtime, true, device_clock_host);\n"
                    << "        if (diag_heartbeat_ms != 0u) dc_diag_heartbeat_enable(runtime, diag_heartbeat_ms);\n"
-                   << "        if (host_window) dc_host_status_enable(runtime, \"DreamcastRecomp 0.0.204 - runner activo\");\n\n"
-                   << "        std::cout << \"DreamcastRecomp 0.0.204 native runner\\n\"\n"
+                   << "        if (host_window) dc_host_status_enable(runtime, \"DreamcastRecomp 0.0.205 - runner activo\");\n\n"
+                   << "        std::cout << \"DreamcastRecomp 0.0.205 native runner\\n\"\n"
                    << "                     \"===================================\\n\"\n"
                    << "                     \"Executing " << function.name << " @ " << hex8(function.entry) << "\\n\\n\";\n\n"
                    << "        runtime.sh4_async_redirect = false;\n"
@@ -21177,7 +21198,7 @@
                    << "#endif\n"
                    << "    std::ostringstream out;\n"
                    << "    if (!ec) out << \"logs/\";\n"
-                   << "    out << \"DreamcastRecomp_0.0.204_session_\" << std::put_time(&tm, \"%Y%m%d-%H%M%S\") << \".log\";\n"
+                   << "    out << \"DreamcastRecomp_0.0.205_session_\" << std::put_time(&tm, \"%Y%m%d-%H%M%S\") << \".log\";\n"
                    << "    return out.str();\n"
                    << "}\n\n"
                    << "class DCRSessionLogGuard final {\n"
@@ -21234,6 +21255,8 @@
                    << "        bool probe_controller_start_burst = false;\n"
                    << "        bool maple_host_input = false;\n"
                    << "        bool commercial_boot = false;\n"
+                   << "        bool direct_game_entry = false;\n"
+                   << "        std::uint32_t direct_game_pc = 0x8C010000u;\n"
                    << "        std::uint64_t commercial_continuation_limit = 0u;\n"
                    << "        std::uint64_t commercial_stages = 0u;\n"
                    << "        std::string disc_map;\n"
@@ -21304,6 +21327,8 @@
                    << "                runtime.sh4_abi_audit = true;\n"
                    << "            } else if (key == \"--commercial-boot\") {\n"
                    << "                commercial_boot = true; runtime.commercial_boot_active = true; // setup already applied in pre-scan\n"
+                   << "            } else if (key.rfind(\"--direct-game-entry\", 0) == 0) {\n"
+                   << "                direct_game_entry = true; direct_game_pc = static_cast<std::uint32_t>(std::stoul(read_value(), nullptr, 0));\n"
                    << "            } else if (key.rfind(\"--commercial-continuation-limit\", 0) == 0) {\n"
                    << "                commercial_continuation_limit = std::stoull(read_value(), nullptr, 0);\n"
                    << "            } else if (key.rfind(\"--disc-map\", 0) == 0) {\n"
@@ -21487,17 +21512,31 @@
                    << "        runtime.fast_dispatch_enabled = fast_dispatch;\n"
                    << "        runtime.direct_dispatch_enabled = direct_dispatch;\n"
                    << "        dc_refresh_dispatch_ready(runtime);\n"
-                   << "        if (pvr_window) dc_pvr_enable_window(runtime, \"DreamcastRecomp 0.0.204 - PVR live\", pvr_window_scale, pvr_window_interval, pvr_window_throttle_ms, pvr_window_fps);\n"
+                   << "        if (pvr_window) dc_pvr_enable_window(runtime, \"DreamcastRecomp 0.0.205 - PVR live\", pvr_window_scale, pvr_window_interval, pvr_window_throttle_ms, pvr_window_fps);\n"
                    << "        runtime.device_clock_host_max_catchup_steps = device_clock_host_max_catchup;\n"
                    << "        if (device_clock) dc_device_clock_enable(runtime, true, device_clock_host);\n"
                    << "        if (diag_heartbeat_ms != 0u) dc_diag_heartbeat_enable(runtime, diag_heartbeat_ms);\n"
-                   << "        if (host_window) dc_host_status_enable(runtime, \"DreamcastRecomp 0.0.204 - runner activo\");\n\n"
-                   << "        std::cout << \"DreamcastRecomp 0.0.204 native runner\\n\"\n"
+                   << "        if (host_window) dc_host_status_enable(runtime, \"DreamcastRecomp 0.0.205 - runner activo\");\n\n"
+                   << "        std::uint32_t dcr_initial_target = " << hex8(program.root_address) << ";\n"
+                   << "        if (direct_game_entry) {\n"
+                   << "            if (!commercial_boot) throw std::runtime_error(\"--direct-game-entry requires --commercial-boot\");\n"
+                   << "            for (auto& reg : ctx.r) reg = 0u;\n"
+                   << "            ctx.r[15] = 0x8C00F400u;\n"
+                   << "            ctx.pr = 0x8C00E0B2u;\n"
+                   << "            ctx.gbr = 0x8C000000u;\n"
+                   << "            ctx.fpul = 0u;\n"
+                   << "            dc_write_sr(ctx, 0x700000F0u);\n"
+                   << "            dc_write_fpscr(ctx, 0x00040000u);\n"
+                   << "            ctx.pc = direct_game_pc;\n"
+                   << "            dcr_initial_target = direct_game_pc;\n"
+                   << "            std::cerr << \"[DCR DIRECT-GAME] bypass bootstrap; entry=0x\" << std::hex << std::uppercase << direct_game_pc << std::dec << \"\\n\";\n"
+                   << "        }\n"
+                   << "        std::cout << \"DreamcastRecomp 0.0.205 native runner\\n\"\n"
                    << "                     \"===================================\\n\"\n"
                    << "                     \"Reachable functions: " << result.functions.size() << "\\n\"\n"
                    << "                     \"Executing " << program.root_name << " @ " << hex8(program.root_address) << "\\n\\n\";\n\n"
                    << "        runtime.sh4_async_redirect = false;\n"
-                   << "        call_recompiled(ctx, runtime, " << hex8(program.root_address) << ");\n"
+                   << "        call_recompiled(ctx, runtime, dcr_initial_target);\n"
                    << "        if (commercial_boot) {\n"
                    << "            while (ctx.pc != kHostReturnSentinel && (commercial_continuation_limit == 0u || commercial_stages < commercial_continuation_limit)) {\n"
                    << "                const std::uint32_t next_pc = ctx.pc;\n"
--- a/src/disc/raw_recompiler.cpp
+++ b/src/disc/raw_recompiler.cpp
@@ -52,7 +52,7 @@
 
 Options parse(int argc, char** argv) {
     if (argc < 2) {
-        std::cout << "dc_raw_recomp 0.0.204 <BOOT.BIN> [--base=0x8C010000] [--entry=0x8C010000] "
+        std::cout << "dc_raw_recomp 0.0.205 <BOOT.BIN> [--base=0x8C010000] [--entry=0x8C010000] "
                      "[--seed=ADDR ...] [--output=DIR] [--map=FILE] [--max-functions=N] [--max-closure-entries=N] [--closure-passes=N] [--no-emit]\n";
         std::exit(1);
     }
@@ -2494,7 +2494,7 @@
                     literal_value_reaches_indirect_call_on_local_path(bytes, o.base, def.address, def.rn);
                 if (!immediate_proof && !cfg_proof) continue;
 
-                // 0.0.204: an actual PC-relative literal value proven to reach
+                // 0.0.205: an actual PC-relative literal value proven to reach
                 // JSR/JMP is stronger executable-entry evidence than the generic
                 // pointer-density heuristic. Tiny Katana leaf callbacks commonly
                 // end in RTS after two setup instructions and place their literal
@@ -3113,7 +3113,7 @@
             emitted = dcrecomp::emit_cpp_program(elf, program, {o.output, true, true, true});
         }
 
-        std::cout << "DreamcastRecomp Raw Commercial Recompiler 0.0.204\n"
+        std::cout << "DreamcastRecomp Raw Commercial Recompiler 0.0.205\n"
                      "=============================================\n"
                   << "Input:                  " << o.path.string() << "\n"
                   << "Image bytes:            " << bytes.size() << "\n"
--- a/run_commercial_recompile.bat
+++ b/run_commercial_recompile.bat
@@ -5,7 +5,7 @@
 if "%~1"=="" (
   echo Uso: run_commercial_recompile.bat "ruta\juego.cdi"
   echo.
-  echo 0.0.204 reproduce el arranque real de consola y amplia la closure comercial sin RAW_SH4: extrae IP.BIN y el boot,
+  echo 0.0.205 reproduce el arranque real de consola y amplia la closure comercial sin RAW_SH4: extrae IP.BIN y el boot,
   echo detecta GD-ROM vs selfboot, prepara la imagen 0x8C008000 y recompila
   echo tanto el bootstrap como la entrada comercial 0x8C010000.
   echo Los datos comerciales quedan solo en generated\ y nunca se redistribuyen.
@@ -47,7 +47,7 @@
 if errorlevel 1 exit /b 3
 
 echo.
-echo [DreamcastRecomp 0.0.204] Preparando boot de consola
+echo [DreamcastRecomp 0.0.205] Preparando boot de consola
 build\Release\dc_boot_prepare.exe ^
   generated\commercial_recompiled\IP.BIN ^
   generated\commercial_recompiled\BOOT.DISC.BIN ^
@@ -57,7 +57,7 @@
 if errorlevel 1 exit /b 4
 
 echo.
-echo [DreamcastRecomp 0.0.204] Recompilacion comercial IP.BIN + juego
+echo [DreamcastRecomp 0.0.205] Recompilacion comercial IP.BIN + juego
 build\Release\dc_raw_recomp.exe generated\commercial_recompiled\BOOTSTRAP.BIN ^
   --base=0x8C008000 ^
   --entry=0x8C008300 ^
--- a/run_commercial_recompiled.bat
+++ b/run_commercial_recompiled.bat
@@ -6,7 +6,7 @@
   echo Uso: run_commercial_recompiled.bat "ruta\juego.cdi"
   echo.
   echo Genera/compila el ejecutable comercial y lo arranca desde el IP.BIN real.
-  echo 0.0.204 conserva la baseline 0.0.91 y usa raster PVR D3D11 + presentacion DXGI directa, con fallback software multihilo.
+  echo 0.0.205 conserva la baseline 0.0.91 y usa raster PVR D3D11 + presentacion DXGI directa, con fallback software multihilo.
   echo Incluye probes separados para background, depth y shading 3D.
   echo Teclas: flechas=D-pad, Z/Space/J=A, X/K=B, C/U=X, V/I=Y, Enter=Start. Solo se leen con la ventana PVR enfocada.
   exit /b 1
@@ -18,11 +18,11 @@
 if errorlevel 1 exit /b %ERRORLEVEL%
 
 echo.
-echo [DreamcastRecomp 0.0.204] Ejecutando bootstrap comercial recompilado...
+echo [DreamcastRecomp 0.0.205] Ejecutando bootstrap comercial recompilado...
 echo [INFO] Revisa fmov64=, pvr-objsrc=, pvr-sqpt=, pvr-sq2w1=, pvr-trisrc= y pvr-onsrc=.
 echo [INFO] Traza CALL/RET completa desactivada: el IP.BIN hace decenas de miles de llamadas al decodificar graficos.
 echo [INFO] Si ocurre un error, se mostraran automaticamente las ultimas 32 llamadas.
-echo [INFO] 0.0.204: D3D11 raster + DXGI direct-present; readback CPU solo para RTT/dumps/fallback.
+echo [INFO] 0.0.205: D3D11 raster + DXGI direct-present; readback CPU solo para RTT/dumps/fallback.
 echo [INFO] FPU AOT: Region+ lineage por defecto (cache FR/XF local probado + FMA3; metricas FPU hot-path apagadas).
 echo [INFO] Superblock 0.0.155 queda disponible con run_commercial_recompiled_fpu_superblock.bat para rollback/A-B.
 echo [INFO] El runner normal NO activa --pvr-profile para evitar medir cada triangulo durante el juego.
@@ -33,7 +33,13 @@
 set DCR_FPU_MODE=regionplus
 set DCR_FPU_METRICS=0
 set DCR_HOT_TRACE=0
+set "DIRECT_GAME_ARG=--commercial-continuation-limit=0"
+if not "%DCR_DIRECT_GAME_ENTRY%"=="" (
+  set "DIRECT_GAME_ARG=--direct-game-entry=%DCR_DIRECT_GAME_ENTRY%"
+  echo [INFO] Direct-game experimental: saltando IP.BIN runtime y entrando en %DCR_DIRECT_GAME_ENTRY%.
+)
 _cb\Release\dreamcast_program.exe ^
+  %DIRECT_GAME_ARG% ^
   --commercial-boot ^
   --disc-map=generated\commercial_recompiled\disc.map ^
   --pvr-window ^
@@ -52,7 +58,7 @@
 set RC=%ERRORLEVEL%
 echo.
 echo [INFO] El runner comercial termino con RC=%RC%.
-echo [INFO] Log completo: %CD%\logs\DreamcastRecomp_0.0.204_session_YYYYMMDD-HHMMSS.log
+echo [INFO] Log completo: %CD%\logs\DreamcastRecomp_0.0.205_session_YYYYMMDD-HHMMSS.log
 if "%RC%"=="130" echo [INFO] RC=130 significa que cerraste manualmente una ventana durante la ejecucion comercial.
 echo [INFO] Si falla, el runner imprime automaticamente las ultimas 32 llamadas antes de [DreamcastRecomp ERROR].
 exit /b %RC%
--- /dev/null
+++ b/run_commercial_recompiled_direct_game.bat
@@ -0,0 +1,15 @@
+@echo off
+setlocal
+cd /d "%~dp0"
+if "%~1"=="" (
+  echo Uso: run_commercial_recompiled_direct_game.bat "ruta\juego.cdi"
+  echo.
+  echo Modo experimental 0.0.205: conserva la extraccion y closure comercial,
+  echo pero salta el bootstrap IP.BIN en runtime y entra directamente a 0x8C010000.
+  echo Util para imagenes con bootstrap/selfboot modificado que vuelve a transformar
+  echo un 1ST_READ.BIN que ya es ejecutable.
+  exit /b 1
+)
+set "DCR_DIRECT_GAME_ENTRY=0x8C010000"
+call run_commercial_recompiled.bat "%~1"
+exit /b %ERRORLEVEL%
