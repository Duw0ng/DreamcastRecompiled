--- /mnt/data/orig204_compare/DreamcastRecomp_0.0.204_rebased193/src/disc/raw_recompiler.cpp	2026-09-15 18:22:32.000000000 +0000
+++ /mnt/data/dcr204_test/DreamcastRecomp_0.0.204_rebased193/src/disc/raw_recompiler.cpp	2026-09-16 06:35:20.721282559 +0000
@@ -703,6 +703,124 @@
     return saw_vbr && saw_source_load && saw_store;
 }
 
+struct IndexedRelocatedCopy {
+    std::uint32_t source{};
+    std::uint32_t destination{};
+    std::uint32_t size{};
+};
+
+std::optional<IndexedRelocatedCopy> indexed_relocated_copy_from_literal(
+    const std::vector<std::uint8_t>& bytes,
+    std::uint32_t base,
+    const dcrecomp::FunctionAnalysis& analysis,
+    const dcrecomp::LiteralReference& source_literal,
+    std::uint8_t source_reg) {
+    using O = dcrecomp::sh4::Opcode;
+
+    const auto source = canonical_local_address(base, bytes.size(), source_literal.value);
+    if (!source) return std::nullopt;
+
+    // Recognize the compact, compiler-generated byte-copy idiom used by retail
+    // bootstrap code to move an executable helper to another RAM alias/region:
+    //
+    //   mov.l source,rS
+    //   mov.l dest,rD
+    //   mov #0,r0
+    //   mov.w bound,rB
+    // loop:
+    //   mov.b @(r0,rS),rV
+    //   mov.b rV,@(r0,rD)
+    //   ... increment r0 ...
+    //   cmp/hi rB,r0
+    //   bf loop
+    //
+    // The old relocation detector only handled VBR setup copies.  This form is
+    // equally strong evidence of executable relocation once the copied source is
+    // a clean local SH-4 entry and the destination is another main-RAM address.
+    const std::uint32_t window_begin =
+        source_literal.instruction_address >= 0x20u ? source_literal.instruction_address - 0x20u : base;
+    const std::uint32_t window_end = static_cast<std::uint32_t>(std::min<std::uint64_t>(
+        static_cast<std::uint64_t>(source_literal.instruction_address) + 0x30u,
+        static_cast<std::uint64_t>(base) + bytes.size()));
+
+    const dcrecomp::sh4::Instruction* indexed_load = nullptr;
+    const dcrecomp::sh4::Instruction* indexed_store = nullptr;
+    for (const auto& insn : analysis.instructions) {
+        if (insn.address < source_literal.instruction_address || insn.address > window_end) continue;
+        if (!indexed_load && insn.opcode == O::MovBIndexedLoad && insn.rm == source_reg) {
+            indexed_load = &insn;
+            continue;
+        }
+        if (indexed_load && insn.address >= indexed_load->address &&
+            insn.address <= indexed_load->address + 6u &&
+            insn.opcode == O::MovBIndexedStore && insn.rm == indexed_load->rn) {
+            indexed_store = &insn;
+            break;
+        }
+    }
+    if (!indexed_load || !indexed_store) return std::nullopt;
+    const std::uint8_t destination_reg = indexed_store->rn;
+
+    const dcrecomp::LiteralReference* destination_literal = nullptr;
+    for (const auto& literal : analysis.literals) {
+        if (literal.kind != dcrecomp::LiteralKind::Long32 || literal.destination_register != destination_reg) continue;
+        if (literal.instruction_address < window_begin ||
+            literal.instruction_address > source_literal.instruction_address + 8u) continue;
+        // Prefer the closest defining literal before the copy loop.
+        if (!destination_literal || literal.instruction_address > destination_literal->instruction_address)
+            destination_literal = &literal;
+    }
+    if (!destination_literal) return std::nullopt;
+
+    const std::uint32_t destination_phys = destination_literal->value & 0x1FFFFFFFu;
+    const std::uint32_t source_phys = (*source) & 0x1FFFFFFFu;
+    // Both ends must be in Dreamcast main RAM, and this must actually relocate
+    // rather than merely copy bytes in-place inside the source image.
+    if (destination_phys < 0x0C000000u || destination_phys >= 0x10000000u ||
+        source_phys < 0x0C000000u || source_phys >= 0x10000000u ||
+        destination_phys == source_phys) return std::nullopt;
+
+    const dcrecomp::sh4::Instruction* compare = nullptr;
+    const dcrecomp::sh4::Instruction* back_branch = nullptr;
+    for (const auto& insn : analysis.instructions) {
+        if (insn.address < indexed_store->address || insn.address > indexed_store->address + 0x18u) continue;
+        if (!compare && insn.opcode == O::CmpHi && insn.rn == 0u) compare = &insn;
+        if ((insn.opcode == O::Bf || insn.opcode == O::Bt ||
+             insn.opcode == O::BfS || insn.opcode == O::BtS) &&
+            insn.target <= indexed_load->address && insn.target + 8u >= indexed_load->address) {
+            back_branch = &insn;
+        }
+    }
+    if (!compare || !back_branch) return std::nullopt;
+
+    std::optional<std::uint32_t> inclusive_bound;
+    for (const auto& literal : analysis.literals) {
+        if (literal.destination_register != compare->rm ||
+            literal.instruction_address > compare->address ||
+            literal.instruction_address + 0x20u < source_literal.instruction_address) continue;
+        if (literal.kind == dcrecomp::LiteralKind::Word16) {
+            inclusive_bound = literal.value & 0xFFFFu;
+        } else if (literal.kind == dcrecomp::LiteralKind::Long32) {
+            inclusive_bound = literal.value;
+        }
+    }
+    if (!inclusive_bound) {
+        for (const auto& insn : analysis.instructions) {
+            if (insn.opcode != O::MovImm || insn.rn != compare->rm ||
+                insn.address > compare->address ||
+                insn.address + 0x20u < source_literal.instruction_address) continue;
+            const auto imm = static_cast<std::int32_t>(insn.immediate);
+            if (imm >= 0) inclusive_bound = static_cast<std::uint32_t>(imm);
+        }
+    }
+    if (!inclusive_bound || *inclusive_bound >= 0x10000u) return std::nullopt;
+    const std::uint32_t copy_size = *inclusive_bound + 1u;
+    if (copy_size < 16u ||
+        static_cast<std::uint64_t>(*source - base) + copy_size > bytes.size()) return std::nullopt;
+
+    return IndexedRelocatedCopy{*source, destination_literal->value, copy_size};
+}
+
 bool literal_target_is_installed_in_vbr_vector(const std::vector<std::uint8_t>& bytes,
                                                std::uint32_t base,
                                                std::uint32_t load_address,
@@ -758,7 +876,10 @@
     if (local_address(base, bytes.size(), address + 2u)) {
         const auto first = dcrecomp::sh4::decode(raw16_at(bytes, base, address), address);
         const auto delay_raw = raw16_at(bytes, base, address + 2u);
-        if (first.opcode == dcrecomp::sh4::Opcode::Rts && delay_raw == 0x0009u) return false;
+        const auto delay = dcrecomp::sh4::decode(delay_raw, address + 2u);
+        const bool canonical_leaf_delay = delay_raw == 0x0009u ||
+            (delay.opcode == dcrecomp::sh4::Opcode::MovImm && delay.rn == 0u);
+        if (first.opcode == dcrecomp::sh4::Opcode::Rts && canonical_leaf_delay) return false;
     }
 
     // Do not reject compact compiler thunks merely because their literal pool
@@ -1057,7 +1178,9 @@
             // appears in SDK initializer/fill APIs. Restrict the zero-length body
             // case to the canonical NOP delay slot so arbitrary data beginning in
             // 0x000B is not broadly promoted as callable code.
-            return d.opcode == dcrecomp::sh4::Opcode::Rts && slot_raw == 0x0009u;
+            return d.opcode == dcrecomp::sh4::Opcode::Rts &&
+                   (slot_raw == 0x0009u ||
+                    (slot.opcode == dcrecomp::sh4::Opcode::MovImm && slot.rn == 0u));
         }
 
         const bool terminal_tail_call = d.opcode == dcrecomp::sh4::Opcode::Jmp ||
@@ -1501,6 +1624,55 @@
     return targets;
 }
 
+
+std::vector<std::uint32_t> long_dense_callback_table_targets(
+    const std::vector<std::uint8_t>& bytes,
+    std::uint32_t base) {
+    std::vector<std::uint32_t> targets;
+    if (bytes.size() < 64u) return targets;
+
+    // Long, contiguous arrays of absolute pointers into decoder-clean SH-4 are
+    // a common SDK interface/vtable representation. Some tables are reached only
+    // through runtime data structures, so none of their members is necessarily
+    // in the initial call closure. A run of at least 8 local pointers with at
+    // least 3/4 of its confidence window independently looking callable is strong
+    // executable evidence without relying on any title-specific address.
+    constexpr std::size_t kMinRun = 8u;
+    constexpr std::size_t kConfidenceMax = 32u;
+    const std::size_t words = bytes.size() / 4u;
+    std::size_t wi = 0u;
+    while (wi < words) {
+        const std::size_t run_start = wi;
+        std::vector<std::uint32_t> run;
+        while (wi < words) {
+            const auto address = static_cast<std::uint32_t>(
+                static_cast<std::uint64_t>(base) + wi * 4u);
+            const auto canonical = canonical_local_address(base, bytes.size(), raw32_at(bytes, base, address));
+            if (!canonical) break;
+            run.push_back(*canonical);
+            ++wi;
+        }
+        if (run.size() >= kMinRun) {
+            const auto confidence = std::min<std::size_t>(run.size(), kConfidenceMax);
+            std::size_t callable = 0u;
+            for (std::size_t i = 0; i < confidence; ++i) {
+                if (!looks_like_pointer_table(bytes, base, run[i]) &&
+                    looks_like_dense_dispatch_entry(bytes, base, run[i])) ++callable;
+            }
+            if (callable * 4u >= confidence * 3u) {
+                for (const auto target : run) {
+                    if (looks_like_pointer_table(bytes, base, target)) continue;
+                    if (looks_like_dense_dispatch_entry(bytes, base, target)) targets.push_back(target);
+                }
+            }
+        }
+        if (wi == run_start) ++wi;
+    }
+    std::sort(targets.begin(), targets.end());
+    targets.erase(std::unique(targets.begin(), targets.end()), targets.end());
+    return targets;
+}
+
 enum class AddressTakenEvidenceStage : std::uint8_t { Candidate = 0u, Proven = 1u };
 
 struct AddressTakenEvidence {
@@ -1994,6 +2166,7 @@
     std::size_t stored_callback_targets{};
     std::size_t callback_object_targets{};
     std::size_t anchored_dense_callback_targets{};
+    std::size_t long_dense_callback_targets{};
     std::size_t strided_callback_targets{};
     std::size_t direct_literal_targets{};
     std::size_t cfg_literal_call_targets{};
@@ -2436,6 +2609,69 @@
                 }
             }
 
+            // Some retail bootstraps copy a complete executable helper to a
+            // fixed main-RAM address with an indexed byte loop and then jump to
+            // that relocated address. Unlike the VBR installer above, the code
+            // is not associated with VBR at all (Record of Lodoss War is one
+            // example), so discover the source template from the copy itself.
+            //
+            // Internal absolute references in such a template are linked for
+            // the *destination* address. Map only those referenced entrypoints
+            // back to the source copy and require a clean SH-4 analysis before
+            // promoting them. This avoids treating literal/data fields in the
+            // copied blob as functions while still compiling every entrypoint
+            // that the relocated helper can dispatch to.
+            for (const auto& def : analysis.instructions) {
+                if (def.opcode != dcrecomp::sh4::Opcode::MovLPcRel) continue;
+                const auto lit = std::find_if(analysis.literals.begin(), analysis.literals.end(),
+                    [&](const dcrecomp::LiteralReference& r) { return r.instruction_address == def.address; });
+                if (lit == analysis.literals.end()) continue;
+                const auto copy = indexed_relocated_copy_from_literal(bytes, o.base, analysis, *lit, def.rn);
+                if (!copy) continue;
+
+                bool promoted_template = false;
+                if (!starts.contains(copy->source) && !additions.contains(copy->source)) {
+                    additions.insert(copy->source);
+                    promoted_template = true;
+                }
+
+                const std::uint32_t destination_phys = copy->destination & 0x1FFFFFFFu;
+                const std::uint64_t source_end64 = static_cast<std::uint64_t>(copy->source) + copy->size;
+                const std::uint32_t source_end = static_cast<std::uint32_t>(source_end64);
+                for (std::uint32_t offset = 0u; offset + 4u <= copy->size; offset += 2u) {
+                    const std::uint32_t word_address = copy->source + offset;
+                    if (!local_address(o.base, bytes.size(), word_address) ||
+                        static_cast<std::uint64_t>(word_address - o.base) + 4u > bytes.size()) continue;
+                    const std::uint32_t value = raw32_at(bytes, o.base, word_address);
+                    const std::uint32_t value_phys = value & 0x1FFFFFFFu;
+                    if (value_phys < destination_phys ||
+                        value_phys >= destination_phys + copy->size) continue;
+                    const std::uint32_t candidate = copy->source + (value_phys - destination_phys);
+                    if ((candidate & 1u) != 0u || candidate < copy->source || candidate >= source_end) continue;
+                    if (starts.contains(candidate) || additions.contains(candidate)) continue;
+
+                    dcrecomp::FunctionAnalysis relocated_entry;
+                    try {
+                        auto probe_starts = starts;
+                        probe_starts.insert(candidate);
+                        const auto probe_elf = make_synthetic_elf(bytes, o.base, o.entry, probe_starts);
+                        relocated_entry = dcrecomp::analyze_function_at(probe_elf, candidate);
+                    } catch (const std::exception&) {
+                        continue;
+                    }
+                    if (relocated_entry.instructions.empty() || relocated_entry.unknown != 0u) continue;
+                    const bool stays_inside_copy = std::all_of(
+                        relocated_entry.instructions.begin(), relocated_entry.instructions.end(),
+                        [&](const dcrecomp::sh4::Instruction& insn) {
+                            return insn.address >= copy->source && insn.address < source_end;
+                        });
+                    if (!stays_inside_copy) continue;
+                    additions.insert(candidate);
+                }
+
+                if (promoted_template) ++stats.relocatable_code_templates;
+            }
+
             // A second common symbol-free pattern is a literal pointer to a small
             // callback object/vtable. The caller loads the object, then fetches one
             // member with MOV.L @(disp,Rn), and finally performs JSR/JMP @Rm. Unlike
@@ -2592,6 +2828,24 @@
             }
         }
 
+
+        // Recover long SDK interface/vtable arrays even when no member is in the
+        // initial closure. The table shape itself is the anchor: >=16 consecutive
+        // local pointers and >=75% decoder-clean callable entries.
+        if (pass == 0u) {
+            for (const auto target : long_dense_callback_table_targets(bytes, o.base)) {
+                if (starts.contains(target) || additions.contains(target)) continue;
+                try {
+                    const auto fragment = dcrecomp::analyze_code_fragment_at(elf, target);
+                    if (fragment.instructions.empty() || fragment.unknown != 0u) continue;
+                } catch (const std::exception&) {
+                    continue;
+                }
+                additions.insert(target);
+                ++stats.long_dense_callback_targets;
+            }
+        }
+
         // 0.0.202: global address-taken proof. dcrecomp demonstrated that many
         // retail callbacks exist only as function pointers in static data/vtables;
         // no nearby JSR literal necessarily exposes them. Scan repeated clustered
@@ -2825,6 +3079,7 @@
                   << "Returned literal targets:" << std::setw(6) << closure.returned_literal_targets << "\n"
                   << "Callback-object targets:" << std::setw(5) << closure.callback_object_targets << "\n"
                   << "Anchored dense callbacks:" << std::setw(5) << closure.anchored_dense_callback_targets << "\n"
+                  << "Long dense callbacks:    " << std::setw(5) << closure.long_dense_callback_targets << "\n"
                   << "Strided callback targets:" << std::setw(5) << closure.strided_callback_targets << "\n"
                   << "Relocatable templates:" << std::setw(8) << closure.relocatable_code_templates << "\n"
                   << "Argument callbacks:" << std::setw(11) << closure.argument_callback_targets << "\n"
--- /mnt/data/orig204_compare/DreamcastRecomp_0.0.204_rebased193/src/codegen/cpp_emitter.cpp	2026-09-15 18:30:44.000000000 +0000
+++ /mnt/data/dcr204_test/DreamcastRecomp_0.0.204_rebased193/src/codegen/cpp_emitter.cpp	2026-09-16 10:33:55.202631278 +0000
@@ -1308,6 +1308,13 @@
 struct DCRuntime {
     static constexpr std::uint32_t kMainRamPhysicalBase = 0x0C000000u;
     static constexpr std::size_t kMainRamSize = 16u * 1024u * 1024u;
+    // PC-relative literals are normally baked into generated code. Retail loaders
+    // may patch their literal pools at runtime (Lodoss stores its saved SP through
+    // a P2 alias and reloads it with MOV.L @(disp,PC)). Track guest-written SDRAM
+    // pages so only modified literal pages fall back to a real RAM read.
+    static constexpr std::size_t kMainRamLiteralDirtyPageShift = 12u;
+    static constexpr std::size_t kMainRamLiteralDirtyPageSize = 1u << kMainRamLiteralDirtyPageShift;
+    static constexpr std::size_t kMainRamLiteralDirtyPages = kMainRamSize / kMainRamLiteralDirtyPageSize;
     static constexpr std::size_t kVramSize = 8u * 1024u * 1024u;
     static constexpr std::size_t kAicaRamSize = 2u * 1024u * 1024u;
     // The retail BIOS font syscall returns 0xA0100020 inside the upper half
@@ -1348,6 +1355,8 @@
     static constexpr std::uint32_t kHollyPhysicalEnd = 0x00600000u;
 
     std::vector<std::uint8_t> main_ram;
+    std::array<std::uint8_t, kMainRamLiteralDirtyPages> main_ram_literal_dirty{};
+    std::uint64_t main_ram_dynamic_literal_reads{};
     std::vector<std::uint8_t> vram;
     std::vector<std::uint8_t> aica_ram;
     std::vector<std::uint8_t> boot_rom_low;
@@ -2729,6 +2738,7 @@
 constexpr std::uint32_t kAsicEventRenderVideoBit = 1u << 0; // End of render: video
 constexpr std::uint32_t kAsicEventRenderIspBit   = 1u << 1; // End of render: ISP
 constexpr std::uint32_t kAsicEventRenderTspBit   = 1u << 2; // End of render: TSP
+constexpr std::uint32_t kAsicEventPvrYuvBit      = 1u << 6; // ASIC_EVT_PVR_YUV_DONE = 0x0006
 constexpr std::uint32_t kAsicEventPvrOpaqueBit   = 1u << 7; // End of opaque list transfer
 constexpr std::uint32_t kAsicEventPvrOpaqueModBit= 1u << 8; // End of opaque modifier list
 constexpr std::uint32_t kAsicEventPvrTransBit    = 1u << 9; // End of translucent list transfer
@@ -4747,6 +4757,8 @@
     return static_cast<std::uint32_t>(pos);
 }
 
+std::uint32_t pvr_yuv_to_argb(std::uint8_t y, std::uint8_t u, std::uint8_t v);
+
 std::uint32_t pvr_sample_texture_texel(const DCRuntime& runtime,
                                        std::uint32_t x, std::uint32_t y) {
     const auto& st = runtime.pvr_state;
@@ -4820,6 +4832,25 @@
         byte_offset64 = static_cast<std::uint64_t>(st.texture_offset) +
             pvr_mip_base_offset_bytes(st) + static_cast<std::uint64_t>(texel_index) * 2ull;
     }
+    if (st.pixel_mode == 3u) {
+        // Native PVR YUV422 textures are packed UYVY: one chroma pair for two
+        // horizontal luma samples. Recompute the pair base so either texel can
+        // recover the shared U/V values. Converter output is non-twiddled; the
+        // stride bit permits video widths such as 320 inside a 512-wide surface.
+        if (!st.nontwiddled) return 0xFFFF00FFu;
+        const std::uint32_t stride = pvr_texture_stride_pixels(runtime, st);
+        const std::uint32_t pair_x = x & ~1u;
+        const std::uint64_t pair_off64 = static_cast<std::uint64_t>(st.texture_offset) +
+            pvr_mip_base_offset_bytes(st) +
+            (static_cast<std::uint64_t>(y) * stride + pair_x) * 2ull;
+        if (pair_off64 + 3ull >= runtime.vram.size()) return 0xFFFF00FFu;
+        const std::size_t pair_off = static_cast<std::size_t>(pair_off64);
+        const std::uint8_t u = runtime.vram[pair_off + 0u];
+        const std::uint8_t y0 = runtime.vram[pair_off + 1u];
+        const std::uint8_t v = runtime.vram[pair_off + 2u];
+        const std::uint8_t y1 = runtime.vram[pair_off + 3u];
+        return pvr_yuv_to_argb((x & 1u) ? y1 : y0, u, v);
+    }
     if (byte_offset64 + 1ull >= runtime.vram.size()) return 0xFFFF00FFu;
     const std::size_t off = static_cast<std::size_t>(byte_offset64);
     const std::uint16_t texel = static_cast<std::uint16_t>(runtime.vram[off]) |
@@ -10570,30 +10601,152 @@
     }
 }
 
+std::uint8_t pvr_yuv_clamp8(int value) {
+    return static_cast<std::uint8_t>(std::clamp(value, 0, 255));
+}
+
+std::uint32_t pvr_yuv_to_argb(std::uint8_t y, std::uint8_t u, std::uint8_t v) {
+    // BT.601 studio-range conversion. The PVR stores YUV422 as UYVY and
+    // performs the equivalent conversion while sampling the texture.
+    const int c = std::max(0, static_cast<int>(y) - 16);
+    const int d = static_cast<int>(u) - 128;
+    const int e = static_cast<int>(v) - 128;
+    const std::uint8_t r = pvr_yuv_clamp8((298 * c + 409 * e + 128) >> 8);
+    const std::uint8_t g = pvr_yuv_clamp8((298 * c - 100 * d - 208 * e + 128) >> 8);
+    const std::uint8_t b = pvr_yuv_clamp8((298 * c + 516 * d + 128) >> 8);
+    return 0xFF000000u | (static_cast<std::uint32_t>(r) << 16u) |
+           (static_cast<std::uint32_t>(g) << 8u) | b;
+}
+
+void pvr_yuv_convert_macroblock_420(DCRuntime& runtime, std::uint32_t src,
+                                    std::size_t dst_base, std::uint32_t mb_index,
+                                    std::uint32_t mb_width) {
+    // PVR YUV420 macroblock input is U(8x8), V(8x8), then Y as four 8x8
+    // quadrants (TL, TR, BL, BR): 64 + 64 + 256 = 384 bytes.
+    const std::uint32_t mb_x = mb_index % mb_width;
+    const std::uint32_t mb_y = mb_index / mb_width;
+    const std::uint32_t width_px = mb_width * 16u;
+    const std::size_t stride_bytes = static_cast<std::size_t>(width_px) * 2u;
+    for (std::uint32_t y = 0u; y < 16u; ++y) {
+        for (std::uint32_t x = 0u; x < 16u; x += 2u) {
+            const std::uint32_t uv_index = (y >> 1u) * 8u + (x >> 1u);
+            const std::uint8_t u = dc_read8(runtime, src + uv_index);
+            const std::uint8_t v = dc_read8(runtime, src + 64u + uv_index);
+            auto y_index = [y](std::uint32_t px) -> std::uint32_t {
+                const std::uint32_t quadrant = (y >= 8u ? 2u : 0u) + (px >= 8u ? 1u : 0u);
+                return 128u + quadrant * 64u + (y & 7u) * 8u + (px & 7u);
+            };
+            const std::uint8_t y0 = dc_read8(runtime, src + y_index(x));
+            const std::uint8_t y1 = dc_read8(runtime, src + y_index(x + 1u));
+            const std::size_t out = (dst_base +
+                (static_cast<std::size_t>(mb_y) * 16u + y) * stride_bytes +
+                (static_cast<std::size_t>(mb_x) * 16u + x) * 2u) &
+                (DCRuntime::kVramSize - 1u);
+            runtime.vram[out] = u;
+            runtime.vram[(out + 1u) & (DCRuntime::kVramSize - 1u)] = y0;
+            runtime.vram[(out + 2u) & (DCRuntime::kVramSize - 1u)] = v;
+            runtime.vram[(out + 3u) & (DCRuntime::kVramSize - 1u)] = y1;
+        }
+    }
+}
+
+void pvr_yuv_convert_macroblock_422(DCRuntime& runtime, std::uint32_t src,
+                                    std::size_t dst_base, std::uint32_t mb_index,
+                                    std::uint32_t mb_width) {
+    // YUV422 planar macroblock: U(16x8), V(16x8), Y four 8x8 quadrants.
+    const std::uint32_t mb_x = mb_index % mb_width;
+    const std::uint32_t mb_y = mb_index / mb_width;
+    const std::uint32_t width_px = mb_width * 16u;
+    const std::size_t stride_bytes = static_cast<std::size_t>(width_px) * 2u;
+    for (std::uint32_t y = 0u; y < 16u; ++y) {
+        for (std::uint32_t x = 0u; x < 16u; x += 2u) {
+            const std::uint32_t uv_index = y * 8u + (x >> 1u);
+            const std::uint8_t u = dc_read8(runtime, src + uv_index);
+            const std::uint8_t v = dc_read8(runtime, src + 128u + uv_index);
+            auto y_index = [y](std::uint32_t px) -> std::uint32_t {
+                const std::uint32_t quadrant = (y >= 8u ? 2u : 0u) + (px >= 8u ? 1u : 0u);
+                return 256u + quadrant * 64u + (y & 7u) * 8u + (px & 7u);
+            };
+            const std::uint8_t y0 = dc_read8(runtime, src + y_index(x));
+            const std::uint8_t y1 = dc_read8(runtime, src + y_index(x + 1u));
+            const std::size_t out = (dst_base +
+                (static_cast<std::size_t>(mb_y) * 16u + y) * stride_bytes +
+                (static_cast<std::size_t>(mb_x) * 16u + x) * 2u) &
+                (DCRuntime::kVramSize - 1u);
+            runtime.vram[out] = u;
+            runtime.vram[(out + 1u) & (DCRuntime::kVramSize - 1u)] = y0;
+            runtime.vram[(out + 2u) & (DCRuntime::kVramSize - 1u)] = v;
+            runtime.vram[(out + 3u) & (DCRuntime::kVramSize - 1u)] = y1;
+        }
+    }
+}
+
+void pvr_yuv_dma_execute(DCRuntime& runtime, std::uint32_t& src, std::uint32_t len) {
+    const auto cfg_it = runtime.mmio32.find(DCRuntime::kPvrRegsPhysicalBase + 0x014Cu);
+    const auto addr_it = runtime.mmio32.find(DCRuntime::kPvrRegsPhysicalBase + 0x0148u);
+    const std::uint32_t cfg = cfg_it == runtime.mmio32.end() ? 0u : cfg_it->second;
+    const std::uint32_t addr = addr_it == runtime.mmio32.end() ? 0u : addr_it->second;
+    const bool input_422 = ((cfg >> 24u) & 1u) != 0u;
+    const std::uint32_t mb_width = (cfg & 0x3Fu) + 1u;
+    const std::uint32_t mb_height = ((cfg >> 8u) & 0x3Fu) + 1u;
+    const std::uint32_t total_mbs = std::max(1u, mb_width * mb_height);
+    const std::uint32_t mb_bytes = input_422 ? 512u : 384u;
+    std::uint32_t converted = runtime.mmio32[DCRuntime::kPvrRegsPhysicalBase + 0x0150u];
+    converted %= total_mbs;
+    const std::size_t dst_base = static_cast<std::size_t>(addr & 0x007FFFFFu);
+    const std::uint32_t full_blocks = len / mb_bytes;
+    const std::uint32_t tail = len % mb_bytes;
+
+    for (std::uint32_t i = 0u; i < full_blocks; ++i) {
+        if (input_422)
+            pvr_yuv_convert_macroblock_422(runtime, src, dst_base, converted, mb_width);
+        else
+            pvr_yuv_convert_macroblock_420(runtime, src, dst_base, converted, mb_width);
+        src += mb_bytes;
+        ++converted;
+        if (converted >= total_mbs) {
+            converted = 0u;
+            holly_raise_event(runtime, 0u, kAsicEventPvrYuvBit);
+        }
+    }
+    // CH2 transfers are 32-byte granular. A partial macroblock is invalid input
+    // to the hardware converter; consume it so DMA state still completes, but
+    // do not fabricate output pixels.
+    src += tail;
+    runtime.mmio32[DCRuntime::kPvrRegsPhysicalBase + 0x0150u] = converted;
+    const std::size_t out_bytes = static_cast<std::size_t>(mb_width) * mb_height * 16u * 16u * 2u;
+    pvr_mark_vram_write(runtime, dst_base, out_bytes);
+}
+
 void ch2_dma_execute(DCRuntime& runtime) {
     if ((runtime.sb_c2dst & 1u) == 0u) return;
-    if ((runtime.dmac_dmaor & 0x00008307u) != 0x00008201u) {
-        // Keep the start bit asserted so guest code can observe that the DMA did
-        // not complete. This mirrors the hardware's invalid-DMAOR failure mode.
-        return;
-    }
+    if ((runtime.dmac_dmaor & 0x00008307u) != 0x00008201u) return;
 
     std::uint32_t src = runtime.dmac_sar2 & 0x1FFFFFE0u;
-    std::uint32_t dst = runtime.sb_c2dstat & 0x01FFFFE0u;
+    const std::uint32_t raw_dst = runtime.sb_c2dstat & 0x1FFFFFE0u;
     std::uint32_t len = runtime.sb_c2dlen;
     if (len == 0u) {
         runtime.sb_c2dst = 0u;
-        runtime.dmac_chcr2 |= 0x2u; // TE
+        runtime.dmac_chcr2 |= 0x2u;
         holly_raise_event(runtime, 0u, kAsicEventCh2DmaBit);
         return;
     }
 
     ++runtime.ch2_dma_runs;
     runtime.ch2_dma_bytes += len;
+    const bool is_ta = raw_dst >= kTaFifoPhysicalBase && raw_dst < kTaFifoPhysicalEnd;
+    const bool is_yuv = raw_dst >= kPvrYuvFifoPhysicalBase && raw_dst < kPvrYuvFifoPhysicalEnd;
+    const bool is_tex64 = raw_dst >= kPvrTexDirect64Base && raw_dst < kPvrTexDirect64Base + kPvrTexDirectSize;
+    const bool is_tex32 = raw_dst >= kPvrTexDirect32Base && raw_dst < kPvrTexDirect32Base + kPvrTexDirectSize;
+
+    if (runtime.ch2_dma_runs <= 24u) {
+        std::cerr << "[CH2] run=" << runtime.ch2_dma_runs
+                  << " src=0x" << std::hex << std::uppercase << src
+                  << " dst=0x" << raw_dst << " len=0x" << len
+                  << std::dec << " kind=" << (is_ta ? "TA" : is_yuv ? "YUV" : (is_tex64 || is_tex32) ? "TEX" : "OTHER") << "\n";
+    }
 
-    if ((dst & 0x01000000u) == 0u) {
-        // Direct display-list DMA into the TA FIFO. The hardware transfers in
-        // 32-byte SQ-sized bursts; preserve partial tails conservatively.
+    if (is_ta) {
         ++runtime.ch2_dma_ta;
         std::array<std::uint8_t, 32> packet{};
         std::uint32_t remaining = len;
@@ -10603,8 +10756,6 @@
             for (std::uint32_t i = 0; i < chunk; ++i) packet[i] = dc_read8(runtime, src + i);
             if (chunk == 32u) pvr_submit_packet(runtime, packet.data(), PVRPacketSource::Ch2Dma, runtime.current_pc);
             else {
-                // Tail writes are uncommon for CH2 but route bytewise into the
-                // existing TA partial-packet accumulator so semantics stay exact.
                 for (std::uint32_t i = 0; i < chunk; ++i) {
                     runtime.ta_partial[runtime.ta_partial_size++] = packet[i];
                     if (runtime.ta_partial_size == 32u) {
@@ -10616,20 +10767,27 @@
             src += chunk;
             remaining -= chunk;
         }
-    } else {
-        // Direct texture DMA. LMMODE chooses the 64/32-bit VRAM aperture on
-        // hardware; both aliases share the same flat 8 MiB backing here.
+    } else if (is_yuv) {
+        pvr_yuv_dma_execute(runtime, src, len);
+        // The converter aperture is a FIFO; C2DSTAT remains the FIFO address.
+        runtime.sb_c2dstat = raw_dst;
+    } else if (is_tex64 || is_tex32) {
         ++runtime.ch2_dma_texture;
-        std::size_t vram_off = static_cast<std::size_t>(dst & 0x007FFFFFu);
+        std::size_t vram_off = static_cast<std::size_t>(raw_dst & 0x007FFFFFu);
         for (std::uint32_t i = 0; i < len; ++i)
             runtime.vram[(vram_off + i) & (DCRuntime::kVramSize - 1u)] = dc_read8(runtime, src + i);
         pvr_mark_vram_write(runtime, vram_off, len);
-        runtime.sb_c2dstat = (dst + len) & 0x01FFFFFE0u;
+        src += len;
+        runtime.sb_c2dstat = (raw_dst + len) & 0x1FFFFFE0u;
+    } else {
+        // Preserve DMA progress for currently unmodelled Channel-2 apertures.
+        src += len;
+        runtime.sb_c2dstat = (raw_dst + len) & 0x1FFFFFE0u;
     }
 
     runtime.dmac_sar2 = src;
     runtime.dmac_dmatcr2 = 0u;
-    runtime.dmac_chcr2 |= 0x2u; // TE = transfer end
+    runtime.dmac_chcr2 |= 0x2u;
     runtime.sb_c2dlen = 0u;
     runtime.sb_c2dst = 0u;
     holly_raise_event(runtime, 0u, kAsicEventCh2DmaBit);
@@ -10814,6 +10972,10 @@
     if (physical == DCRuntime::kPvrRegsPhysicalBase + 0x0138u) {
         return runtime.pvr_ta_current_pointer;
     }
+    if (physical == DCRuntime::kPvrRegsPhysicalBase + 0x0150u) {
+        const auto it = runtime.mmio32.find(physical);
+        return it == runtime.mmio32.end() ? 0u : it->second;
+    }
     if (physical == kSystemBusRevisionPhysical) {
         // SB_SBREV (System Bus revision). Katana/Ninja rejects revision values
         // below 10 during njInitSystem and enters its fatal reset loop. Retail
@@ -10940,7 +11102,13 @@
             if (((old_value ^ value) & (1u << 23u)) != 0u) pvr_spg_resync(runtime);
             return;
         }
-        if (pvr_off == 0x0108u) {
+        if (pvr_off == 0x0148u) {
+            runtime.mmio32[physical & ~3u] = value & 0x00FFFFFFu;
+            runtime.mmio32[DCRuntime::kPvrRegsPhysicalBase + 0x0150u] = 0u;
+        } else if (pvr_off == 0x014Cu) {
+            runtime.mmio32[physical & ~3u] = value;
+            runtime.mmio32[DCRuntime::kPvrRegsPhysicalBase + 0x0150u] = 0u;
+        } else if (pvr_off == 0x0108u) {
             runtime.pvr_palette_format = static_cast<std::uint8_t>(value & 3u);
             ++runtime.pvr_palette_write_epoch;
             runtime.pvr_deferred_cached_state_index = 0xFFFFFFFFu;
@@ -11154,6 +11322,56 @@
     std::size_t target_index = 0u;
     if (!main_ram_range(target, kSignatureBytes, target_index)) return false;
 
+    // Once one entry in a relocated code block has been matched, prefer the
+    // already-proven relocation delta for later internal entrypoints.  A pure
+    // 32-byte signature search can be ambiguous when the copied helper contains
+    // two identical prologues (common in Katana bootstrap/cache helpers).  The
+    // destination/source delta disambiguates those entries without any
+    // title-specific address knowledge; the byte comparison remains as a safety
+    // check before accepting the derived source entry.
+    const std::uint32_t target_phys = target & 0x1FFFFFFFu;
+    std::uint32_t delta_match = 0u;
+    std::size_t delta_match_votes = 0u;
+    bool delta_conflict = false;
+    for (const auto& [relocated, source_base] : runtime.relocated_code_aliases) {
+        const std::uint32_t relocated_phys = relocated & 0x1FFFFFFFu;
+        const std::int64_t delta = static_cast<std::int64_t>(target_phys) -
+                                   static_cast<std::int64_t>(relocated_phys);
+        if (delta < -0x100000ll || delta > 0x100000ll) continue;
+        const std::int64_t candidate64 = static_cast<std::int64_t>(source_base) + delta;
+        if (candidate64 < 0 || candidate64 > 0xFFFFFFFFll) continue;
+        const auto candidate = static_cast<std::uint32_t>(candidate64);
+        const auto registered = runtime.targets.find(candidate);
+        if (registered == runtime.targets.end() || registered->second == nullptr) continue;
+        if (delta_match == 0u) {
+            delta_match = candidate;
+            delta_match_votes = 1u;
+        } else if (delta_match == candidate) {
+            ++delta_match_votes;
+        } else {
+            delta_conflict = true;
+        }
+    }
+    if (delta_match != 0u && !delta_conflict) {
+        // Two independently established aliases agreeing on the same source
+        // entry prove the relocation delta even if the original source bytes
+        // have since been overwritten by the bootstrap itself.
+        if (delta_match_votes >= 2u) {
+            source_out = delta_match;
+            return true;
+        }
+        // With only one prior alias, retain the original byte-signature safety
+        // check before extending the relocation mapping.
+        std::size_t source_index = 0u;
+        if (main_ram_range(delta_match, kSignatureBytes, source_index) &&
+            std::memcmp(runtime.main_ram.data() + target_index,
+                        runtime.main_ram.data() + source_index,
+                        kSignatureBytes) == 0) {
+            source_out = delta_match;
+            return true;
+        }
+    }
+
     std::uint32_t match = 0u;
     std::size_t matches = 0u;
     for (const auto& [source, fn] : runtime.targets) {
@@ -11959,7 +12177,11 @@
     }
     std::size_t i = 0;
     if (onchip_ram_range(address, 1, i)) { runtime.onchip_ram[i] = value; return; }
-    if (main_ram_range(address, 1, i)) { runtime.main_ram[i] = value; return; }
+    if (main_ram_range(address, 1, i)) {
+        runtime.main_ram[i] = value;
+        runtime.main_ram_literal_dirty[i >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
+        return;
+    }
     if (vram_range(address, 1, i)) { runtime.vram[i] = value; pvr_mark_vram_write(runtime, i, 1u); return; }
     if (aica_ram_range(address, 1, i)) { runtime.aica_ram[i] = value; ++runtime.aica_sh4_aica_ram_write_epoch; ++runtime.aica_sh4_aica_ram_bytes_written; aica_arm7_touch(runtime); return; }
     if (is_holly_mmio(address) || is_aica_mmio(address) || is_aica_rtc_mmio(address) || is_sh4_internal_mmio(address)) {
@@ -11989,6 +12211,8 @@
     if (main_ram_range(address, 2, i)) {
         runtime.main_ram[i + 0] = static_cast<std::uint8_t>(value & 0xFFu);
         runtime.main_ram[i + 1] = static_cast<std::uint8_t>((value >> 8) & 0xFFu);
+        runtime.main_ram_literal_dirty[i >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
+        runtime.main_ram_literal_dirty[(i + 1u) >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
         return;
     }
     if (vram_range(address, 2, i)) {
@@ -12042,6 +12266,8 @@
         runtime.main_ram[i + 1] = static_cast<std::uint8_t>((value >> 8) & 0xFFu);
         runtime.main_ram[i + 2] = static_cast<std::uint8_t>((value >> 16) & 0xFFu);
         runtime.main_ram[i + 3] = static_cast<std::uint8_t>((value >> 24) & 0xFFu);
+        runtime.main_ram_literal_dirty[i >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
+        runtime.main_ram_literal_dirty[(i + 3u) >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
         return;
     }
     if (vram_range(address, 4, i)) {
@@ -12109,6 +12335,8 @@
             m[1] = static_cast<std::uint8_t>(value >> 8u);
             m[2] = static_cast<std::uint8_t>(value >> 16u);
             m[3] = static_cast<std::uint8_t>(value >> 24u);
+            runtime.main_ram_literal_dirty[off >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
+            runtime.main_ram_literal_dirty[(off + 3u) >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u;
             return;
         }
     }
@@ -14193,30 +14421,36 @@
 }
 
 void dc_sync_host_fpu_mode(std::uint32_t fpscr) {
-    // 0.0.200: SH-4 FPSCR.DN makes denormalized floating-point values behave
-    // as zero. Flycast mirrors this state into the host FP control register;
-    // do the same here so comparisons and transforms cannot diverge merely
-    // because the native x86 FPU preserved a subnormal value. Only DN is
-    // synchronized here: alternate RM modes remain guarded explicitly by
-    // dc_require_fpu_round_nearest until their exception/rounding semantics
-    // are implemented end-to-end.
+    // 0.0.205: mirror the two architecturally-defined SH-4 rounding modes as
+    // well as FPSCR.DN.  RM=00 is round-to-nearest/even and RM=01 is
+    // round-toward-zero; RM=10/11 remain reserved and are rejected when an
+    // arithmetic instruction actually consumes the mode.
     constexpr std::uint32_t kFpscrDn = 1u << 18;
     const bool flush_denormals = (fpscr & kFpscrDn) != 0u;
-    static thread_local int cached_dn = -1;
-    const int wanted = flush_denormals ? 1 : 0;
-    if (cached_dn == wanted) return;
+    const std::uint32_t rm = fpscr & 0x3u;
+    const bool round_to_zero = rm == 1u;
+    static thread_local int cached_mode = -1;
+    const int wanted = (flush_denormals ? 2 : 0) | (round_to_zero ? 1 : 0);
+    if (cached_mode == wanted) return;
 #if defined(_MSC_VER)
     _controlfp(flush_denormals ? _DN_FLUSH : _DN_SAVE, _MCW_DN);
-#elif DCR_HAS_SSE2
+    _controlfp(round_to_zero ? _RC_CHOP : _RC_NEAR, _MCW_RC);
+#endif
+#if DCR_HAS_SSE2
     unsigned int csr = _mm_getcsr();
     constexpr unsigned int kMxcsrFlushToZero = 1u << 15;
-    const unsigned int next = flush_denormals ? (csr | kMxcsrFlushToZero)
-                                              : (csr & ~kMxcsrFlushToZero);
+    constexpr unsigned int kMxcsrRoundingMask = 3u << 13;
+    constexpr unsigned int kMxcsrRoundTowardZero = 3u << 13;
+    unsigned int next = flush_denormals ? (csr | kMxcsrFlushToZero)
+                                        : (csr & ~kMxcsrFlushToZero);
+    next = (next & ~kMxcsrRoundingMask) |
+           (round_to_zero ? kMxcsrRoundTowardZero : 0u);
     if (next != csr) _mm_setcsr(next);
 #else
     (void)flush_denormals;
+    (void)round_to_zero;
 #endif
-    cached_dn = wanted;
+    cached_mode = wanted;
 }
 
 void dc_write_fpscr(SH4Context& ctx, std::uint32_t value) {
@@ -14225,11 +14459,10 @@
 }
 
 void dc_require_fpu_round_nearest(const SH4Context& ctx, std::uint32_t source_address) {
-    // 0.0.27 models the normal IEEE round-to-nearest arithmetic path. Exception
-    // cause/flag updates and alternate FPSCR.RM modes are intentionally explicit
-    // future work rather than silently approximated.
-    if ((ctx.fpscr & 0x3u) != 0u)
-        dc_unimplemented(source_address, "FPU arithmetic with FPSCR.RM != round-to-nearest");
+    // Legacy helper name retained for generated-code compatibility. SH-4 defines
+    // RM=00 (nearest/even) and RM=01 (toward zero); RM=10/11 are reserved.
+    if ((ctx.fpscr & 0x3u) > 1u)
+        dc_unimplemented(source_address, "FPU arithmetic with reserved FPSCR.RM");
 }
 
 void dc_require_fmov32(const SH4Context& ctx, std::uint32_t source_address) {
@@ -15851,16 +16084,45 @@
 
 std::uint32_t dc_load_pc_literal32(DCRuntime& runtime, std::uint32_t source_address,
                                    std::uint32_t storage_address, std::uint32_t baked_value) {
-    if (runtime.active_relocation_source == 0u || runtime.active_relocation_target == 0u) return baked_value;
-    return dc_read32(runtime, dc_relocate_pc_address(runtime, source_address, storage_address));
+    if (runtime.active_relocation_source != 0u && runtime.active_relocation_target != 0u)
+        return dc_read32(runtime, dc_relocate_pc_address(runtime, source_address, storage_address));
+
+    // Preserve the zero-cost baked fast path until guest code has written to the
+    // SDRAM page that owns this literal. P1/P2 aliases collapse to the same
+    // physical offset, so uncached loader patches are observed by cached code.
+    const std::uint32_t physical = storage_address & 0x1FFFFFFFu;
+    if (physical >= DCRuntime::kMainRamPhysicalBase && physical < 0x10000000u) {
+        const std::uint32_t off = (physical - DCRuntime::kMainRamPhysicalBase) &
+                                  static_cast<std::uint32_t>(DCRuntime::kMainRamSize - 1u);
+        if (off <= DCRuntime::kMainRamSize - 4u &&
+            (runtime.main_ram_literal_dirty[off >> DCRuntime::kMainRamLiteralDirtyPageShift] != 0u ||
+             runtime.main_ram_literal_dirty[(off + 3u) >> DCRuntime::kMainRamLiteralDirtyPageShift] != 0u)) {
+            ++runtime.main_ram_dynamic_literal_reads;
+            return dc_read32(runtime, storage_address);
+        }
+    }
+    return baked_value;
 }
 
 std::uint32_t dc_load_pc_literal16(DCRuntime& runtime, std::uint32_t source_address,
                                    std::uint32_t storage_address, std::uint16_t baked_value) {
-    if (runtime.active_relocation_source == 0u || runtime.active_relocation_target == 0u)
-        return static_cast<std::uint32_t>(static_cast<std::int32_t>(static_cast<std::int16_t>(baked_value)));
-    const auto raw = dc_read16(runtime, dc_relocate_pc_address(runtime, source_address, storage_address));
-    return static_cast<std::uint32_t>(static_cast<std::int32_t>(static_cast<std::int16_t>(raw)));
+    if (runtime.active_relocation_source != 0u && runtime.active_relocation_target != 0u) {
+        const auto raw = dc_read16(runtime, dc_relocate_pc_address(runtime, source_address, storage_address));
+        return static_cast<std::uint32_t>(static_cast<std::int32_t>(static_cast<std::int16_t>(raw)));
+    }
+    const std::uint32_t physical = storage_address & 0x1FFFFFFFu;
+    if (physical >= DCRuntime::kMainRamPhysicalBase && physical < 0x10000000u) {
+        const std::uint32_t off = (physical - DCRuntime::kMainRamPhysicalBase) &
+                                  static_cast<std::uint32_t>(DCRuntime::kMainRamSize - 1u);
+        if (off <= DCRuntime::kMainRamSize - 2u &&
+            (runtime.main_ram_literal_dirty[off >> DCRuntime::kMainRamLiteralDirtyPageShift] != 0u ||
+             runtime.main_ram_literal_dirty[(off + 1u) >> DCRuntime::kMainRamLiteralDirtyPageShift] != 0u)) {
+            ++runtime.main_ram_dynamic_literal_reads;
+            const auto raw = dc_read16(runtime, storage_address);
+            return static_cast<std::uint32_t>(static_cast<std::int32_t>(static_cast<std::int16_t>(raw)));
+        }
+    }
+    return static_cast<std::uint32_t>(static_cast<std::int32_t>(static_cast<std::int16_t>(baked_value)));
 }
 
 std::uint32_t dc_canonical_code_target(const DCRuntime& runtime, std::uint32_t target) {
@@ -17863,7 +18125,7 @@
             // for MSVC in 0.0.109/0.0.110). Resolve the architectural FR bank
             // once, preserve PR/RM traps, and retain single-rounding FMAC through dc_host_fma().
             out << "    ++runtime.fpu_fmac_ops;\n"
-                << "    if ((ctx.fpscr & 0x3u) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FPU arithmetic with FPSCR.RM != round-to-nearest\");\n"
+                << "    dc_require_fpu_round_nearest(ctx, " << hex8(i.source_address) << ");\n"
                 << "    if ((ctx.fpscr & (1u << 19)) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FMAC is only available when FPSCR.PR=0\");\n"
                 << "    { auto& frb = ((ctx.fpscr & (1u << 21)) != 0u) ? ctx.xf_bits : ctx.fr_bits; "
                 << "const float f0 = std::bit_cast<float>(frb[0u]); "
@@ -17905,7 +18167,7 @@
             // reference path, but resolve FPSCR and the FR bank locally so the
             // instruction no longer crosses four tiny runtime helpers.
             out << "    ++runtime.fpu_fsrra_ops;\n"
-                << "    if ((ctx.fpscr & 0x3u) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FPU arithmetic with FPSCR.RM != round-to-nearest\");\n"
+                << "    dc_require_fpu_round_nearest(ctx, " << hex8(i.source_address) << ");\n"
                 << "    if ((ctx.fpscr & (1u << 19)) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FSRRA requires FPSCR.PR=0\");\n"
                 << "    { auto& frb = ((ctx.fpscr & (1u << 21)) != 0u) ? ctx.xf_bits : ctx.fr_bits; const float x = std::bit_cast<float>(frb[" << static_cast<unsigned>(i.dst) << "u]); frb[" << static_cast<unsigned>(i.dst) << "u] = std::bit_cast<std::uint32_t>(1.0f / std::sqrt(x)); }\n";
             break;
@@ -17934,7 +18196,7 @@
             // writeback.  Emit the dot product directly so the host compiler
             // sees four scalar loads and one expression instead of a loop.
             out << "    ++runtime.fpu_fipr_ops;\n"
-                << "    if ((ctx.fpscr & 0x3u) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FPU arithmetic with FPSCR.RM != round-to-nearest\");\n"
+                << "    dc_require_fpu_round_nearest(ctx, " << hex8(i.source_address) << ");\n"
                 << "    if ((ctx.fpscr & (1u << 19)) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FIPR requires FPSCR.PR=0\");\n"
                 << "    { auto& frb = ((ctx.fpscr & (1u << 21)) != 0u) ? ctx.xf_bits : ctx.fr_bits; "
                 << "const float a0 = std::bit_cast<float>(frb[" << static_cast<unsigned>(i.src + 0u) << "u]); "
@@ -17955,7 +18217,7 @@
             // rebuilding temporary v[4]/m[16]/o[4] arrays and nested loops for
             // every FTRV while preserving FR/XF bank semantics under FPSCR.FR.
             out << "    ++runtime.fpu_ftrv_ops; if ((ctx.fpscr & (1u << 21)) != 0u) ++runtime.fpu_ftrv_fr1_ops;\n"
-                << "    if ((ctx.fpscr & 0x3u) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FPU arithmetic with FPSCR.RM != round-to-nearest\");\n"
+                << "    dc_require_fpu_round_nearest(ctx, " << hex8(i.source_address) << ");\n"
                 << "    if ((ctx.fpscr & (1u << 19)) != 0u) dc_unimplemented(" << hex8(i.source_address) << ", \"FTRV requires FPSCR.PR=0\");\n"
                 << "    { auto& frb = ((ctx.fpscr & (1u << 21)) != 0u) ? ctx.xf_bits : ctx.fr_bits; const auto& xfb = ((ctx.fpscr & (1u << 21)) != 0u) ? ctx.fr_bits : ctx.xf_bits; "
                 << "const float v0 = std::bit_cast<float>(frb[" << static_cast<unsigned>(i.dst + 0u) << "u]); "
@@ -19532,7 +19794,7 @@
             << "    const std::uint32_t physical = address & 0x1FFFFFFFu;\n"
             << "    if (physical >= DCRuntime::kMainRamPhysicalBase && physical < 0x10000000u) {\n"
             << "        const std::uint32_t off = (physical - DCRuntime::kMainRamPhysicalBase) & static_cast<std::uint32_t>(DCRuntime::kMainRamSize - 1u);\n"
-            << "        if (off <= DCRuntime::kMainRamSize - 4u) { std::memcpy(runtime.main_ram.data() + off, &value, sizeof(value)); return; }\n"
+            << "        if (off <= DCRuntime::kMainRamSize - 4u) { std::memcpy(runtime.main_ram.data() + off, &value, sizeof(value)); runtime.main_ram_literal_dirty[off >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u; runtime.main_ram_literal_dirty[(off + 3u) >> DCRuntime::kMainRamLiteralDirtyPageShift] = 1u; return; }\n"
             << "    }\n"
             << "#endif\n"
             << "    dc_write32_hot(runtime, address, value);\n"
@@ -20240,7 +20502,7 @@
                    << "            } else if (key.rfind(\"--r\", 0) == 0) {\n"
                    << "                const std::string reg_text = key.substr(3, eq == std::string::npos ? std::string::npos : eq - 3);\n"
                    << "                const int reg = std::stoi(reg_text);\n"
-                   << "                if (reg < 0 || reg > 14) throw std::runtime_error(\"Runner register must be R0-R14\");\n"
+                   << "                if (reg < 0 || reg > 15) throw std::runtime_error(\"Runner register must be R0-R15\");\n"
                    << "                ctx.r[reg] = static_cast<std::uint32_t>(std::stoul(read_value(), nullptr, 0));\n"
                    << "            } else if (key.rfind(\"--fpul\", 0) == 0) {\n"
                    << "                ctx.fpul = static_cast<std::uint32_t>(std::stoul(read_value(), nullptr, 0));\n"
@@ -21187,7 +21449,7 @@
                    << "            } else if (key.rfind(\"--r\", 0) == 0) {\n"
                    << "                const std::string reg_text = key.substr(3, eq == std::string::npos ? std::string::npos : eq - 3);\n"
                    << "                const int reg = std::stoi(reg_text);\n"
-                   << "                if (reg < 0 || reg > 14) throw std::runtime_error(\"Runner register must be R0-R14\");\n"
+                   << "                if (reg < 0 || reg > 15) throw std::runtime_error(\"Runner register must be R0-R15\");\n"
                    << "                ctx.r[reg] = static_cast<std::uint32_t>(std::stoul(read_value(), nullptr, 0));\n"
                    << "            } else if (key.rfind(\"--fpul\", 0) == 0) {\n"
                    << "                ctx.fpul = static_cast<std::uint32_t>(std::stoul(read_value(), nullptr, 0));\n"
